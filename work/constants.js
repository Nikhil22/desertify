
/*
 * NOTE
 * time is represented in terms of # of arrivals/exits
 * revenue is represented in terms of dollars ($)
 */
const constants = Object.freeze({
  MAX_WAIT_TIME:  2,
  AVG_REVENUE_PER_CUSTOMER: 20,
  ENTERS_PER_EXIT: 4,
});

module.exports = constants;
